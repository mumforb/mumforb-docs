create -f filename.yml --save-config
describe pod [pod-name]
apply -f filename.yml
kubectl describe - name
kubectl exec -it sh - you're inside the pod via shell
kubectl edit -f filename.yml - live editor in the cli
